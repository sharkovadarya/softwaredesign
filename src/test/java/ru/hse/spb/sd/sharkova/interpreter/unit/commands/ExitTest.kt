package ru.hse.spb.sd.sharkova.interpreter.unit.commands

import org.junit.Assert.fail
import org.junit.Test
import ru.hse.spb.sd.sharkova.interpreter.commands.ExitCommand
import ru.hse.spb.sd.sharkova.interpreter.stream.InputStream

// run this test separately since it exits the program

/*
class ExitTest : CommandTest() {

    @Test
    fun testExit() {
        val command = constructCommand()
        command.execute()
        // this point should not be reached
        fail()
    }

    private fun constructCommand() = ExitCommand(InputStream(emptyList()), outputStream, errorStream)
}*/
